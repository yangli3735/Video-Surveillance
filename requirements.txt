# =============================================================================
#  requirements.txt — Week 1 Python Dependencies
#  Target: Jetson Orin Nano · L4T Docker container
# =============================================================================
#
#  DO NOT add torch, torchvision, or opencv-python here.
#
#  • torch / torchvision are pre-installed in the L4T base image as
#    Jetson-specific aarch64 + CUDA wheels.  Listing them here would
#    cause pip to pull generic PyPI builds that lack CUDA support.
#    The Dockerfile uses a constraint file to enforce this.
#
#  • opencv-python is intentionally excluded.  The L4T base image ships
#    a system-built OpenCV with GStreamer and CUDA support.  The
#    Dockerfile removes any pip-installed opencv after installing
#    ultralytics so the system cv2 takes precedence.
#
# =============================================================================

ultralytics>=8.2.0
pyyaml>=6.0
